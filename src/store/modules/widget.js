const widget = {
  namespaced: true,
  state: {
    isOpen: true
  },
  mutations: {
    setIsOpen(state) {
      state.isOpen = !state.isOpen
    }
  },
  actions: {
    
  },
}

export default widget